<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="w-full h-full bg-zinc-900 p-10 text-white">
        <div class="nav mb-10">
            <a class="text-white px-5 py-2 bg-zinc-500 rounded-lg" href="/read">See all Tasks</a>
        </div>

        <h1 class="text-3xl tracking-tighter mb-3">Update Task</h1>
        <form class="flex flex-col gap-4 max-w-sm " action="/update/<%= todo._id%>" method="post">
            <input class="px-5 py-2 border-2 border-zinc-800 outline-none rounded-lg bg-transparent" type="text"
                name="task" placeholder="write task name" value="<%= todo.task %>" required />
            <textarea class="px-5 py-2 border-2 border-zinc-800 outline-none rounded-lg bg-transparent" name="detail"
                placeholder="task details" required><%= todo.detail %></textarea>

            <input class="px-5 py-2 bg-yellow-500 text-black rounded-lg ml-3" type="submit" value="Update Task">
        </form>
    </div>
</body>

</html>